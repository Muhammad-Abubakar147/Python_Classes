#Built in function is pre define function in python
#We can say that" A built-in function in Python is a pre-defined function provided by Python itself."

print("Hello, World!")

numbers = [10, 20, 30]

print(len(numbers))      # 3
print(max(numbers))      # 30
print(sum(numbers))      # 60
