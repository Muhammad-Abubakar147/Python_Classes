What is recursion ?

                   When a function calls itself repeatdly known as recursion .

We can say that recursion is dangerous version of Loop .
We can do all those work with recursion which can do with loop .